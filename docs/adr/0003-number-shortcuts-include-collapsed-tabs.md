# Count collapsed tabs in number shortcuts

⌘1–⌘9 continue to count all open tabs, including tabs inside collapsed groups. Search opens the group when the shortcut selects one of its tabs. This keeps the shortcut order tied to the tab order, even when some tabs are not visible.
