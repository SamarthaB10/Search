# Number default group names by current order

Groups without a user-set name show "Group N," where N counts only those groups in their current order. Search saves whether a user set the name; the text alone does not decide this. Removing or moving a group changes default names, while a user-set name stays fixed even when its text looks like a default name. This avoids duplicate generated names while letting the numbers describe the groups that exist now.
